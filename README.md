cd predictive-ml-pipeline
   git init
   git add .
   git commit -m "Initial commit: end-to-end predictive maintenance ML pipeline"
   git branch -M main
   git remote add origin https://github.com/khasim8055/predictive-ml-pipeline.git
   git push -u origin main
